import { StyleSheet } from "react-native";

export default StyleSheet.create({
  webView: {
    width: "100%",
    height: 250,
    borderRadius: 0,
    overflow: "hidden",
    backgroundColor: "transparent",
  },
});
