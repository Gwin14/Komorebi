import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    padding: 8,
  },
  buttonPressed: {
    opacity: 0.7,
  },
  buttonDisabled: {
    opacity: 0.5,
  },
});
