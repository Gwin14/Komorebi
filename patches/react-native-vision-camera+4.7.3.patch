diff --git a/node_modules/react-native-vision-camera/ios/Core/CameraSession+Configuration.swift b/node_modules/react-native-vision-camera/ios/Core/CameraSession+Configuration.swift
index ecd2a94..0d17b37 100644
--- a/node_modules/react-native-vision-camera/ios/Core/CameraSession+Configuration.swift
+++ b/node_modules/react-native-vision-camera/ios/Core/CameraSession+Configuration.swift
@@ -89,6 +89,12 @@ extension CameraSession {
       if photoOutput.isPortraitEffectsMatteDeliverySupported {
         photoOutput.isPortraitEffectsMatteDeliveryEnabled = photo.enablePortraitEffectsMatte
       }
+      if photoOutput.isLivePhotoCaptureSupported {
+        photoOutput.isLivePhotoCaptureEnabled = true
+      }
+      if #available(iOS 14.3, *), photoOutput.isAppleProRAWSupported {
+        photoOutput.isAppleProRAWEnabled = true
+      }
       photoOutput.isMirrored = configuration.isMirrored
       // TODO: Enable isResponsiveCaptureEnabled? (iOS 17+)
       // TODO: Enable isFastCapturePrioritizationEnabled? (iOS 17+)
@@ -240,6 +246,16 @@ extension CameraSession {
     } else {
       photoOutput.isHighResolutionCaptureEnabled = true
     }
+
+    if #available(iOS 14.3, *) {
+      if photoOutput.isAppleProRAWSupported && !photoOutput.isAppleProRAWEnabled {
+        photoOutput.isAppleProRAWEnabled = true
+      }
+
+      VisionLogger.log(level: .info, message: "Photo RAW support: appleProRAWSupported=\(photoOutput.isAppleProRAWSupported), appleProRAWEnabled=\(photoOutput.isAppleProRAWEnabled), rawFormats=\(photoOutput.availableRawPhotoPixelFormatTypes), rawFileTypes=\(photoOutput.availableRawPhotoFileTypes)")
+    } else {
+      VisionLogger.log(level: .info, message: "Photo RAW support: rawFormats=\(photoOutput.availableRawPhotoPixelFormatTypes), rawFileTypes=\(photoOutput.availableRawPhotoFileTypes)")
+    }
   }

   // pragma MARK: Side-Props
diff --git a/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift b/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift
index 12f5c07..f9548a6 100644
--- a/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift
+++ b/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift
@@ -39,7 +39,31 @@ extension CameraSession {
       VisionLogger.log(level: .info, message: "Capturing photo...")

       // Create photo settings
-      let photoSettings = AVCapturePhotoSettings()
+      let photoSettings: AVCapturePhotoSettings
+      if options.rawMode != "off" {
+        let availableFormats = photoOutput.availableRawPhotoPixelFormatTypes
+        guard let rawPixelFormat = self.getRawPixelFormat(for: options.rawMode, availableFormats: availableFormats) else {
+          let activeFormat = videoDeviceInput.device.activeFormat
+          let dimensions = CMVideoFormatDescriptionGetDimensions(activeFormat.formatDescription)
+          let supportDescription = self.getRawSupportDescription(for: options.rawMode, availableFormats: availableFormats)
+          promise.reject(error: .capture(.unknown(message: "\(supportDescription) rawFormats=\(availableFormats), activeFormat=\(dimensions.width)x\(dimensions.height), device=\(videoDeviceInput.device.localizedName)")))
+          return
+        }
+
+        let processedFormat = [AVVideoCodecKey: AVVideoCodecType.jpeg]
+        photoSettings = AVCapturePhotoSettings(rawPixelFormatType: rawPixelFormat,
+                                               rawFileType: .dng,
+                                               processedFormat: processedFormat,
+                                               processedFileType: AVFileType("public.jpeg"))
+        if #available(iOS 12.0, *),
+           photoSettings.availableRawEmbeddedThumbnailPhotoCodecTypes.contains(.jpeg) {
+          photoSettings.rawEmbeddedThumbnailPhotoFormat = [
+            AVVideoCodecKey: AVVideoCodecType.jpeg
+          ]
+        }
+      } else {
+        photoSettings = AVCapturePhotoSettings()
+      }

       // set photo resolution
       if #available(iOS 16.0, *) {
@@ -50,8 +74,24 @@ extension CameraSession {

       // depth data
       photoSettings.isDepthDataDeliveryEnabled = photoOutput.isDepthDataDeliveryEnabled
+      photoSettings.embedsDepthDataInPhoto = photoOutput.isDepthDataDeliveryEnabled
       if #available(iOS 12.0, *) {
         photoSettings.isPortraitEffectsMatteDeliveryEnabled = photoOutput.isPortraitEffectsMatteDeliveryEnabled
+        photoSettings.embedsPortraitEffectsMatteInPhoto = photoOutput.isPortraitEffectsMatteDeliveryEnabled
+      }
+
+      // Live Photo paired movie
+      if options.livePhoto {
+        guard options.rawMode == "off" else {
+          promise.reject(error: .capture(.unknown(message: "Live Photo capture cannot be combined with RAW capture.")))
+          return
+        }
+        guard photoOutput.isLivePhotoCaptureSupported,
+              let livePhotoMoviePath = options.livePhotoMoviePath else {
+          promise.reject(error: .capture(.unknown(message: "Live Photo capture is not supported by this camera configuration.")))
+          return
+        }
+        photoSettings.livePhotoMovieFileURL = livePhotoMoviePath
       }

       // quality prioritization
@@ -84,11 +124,52 @@ extension CameraSession {
                                                       enableShutterSound: options.enableShutterSound,
                                                       metadataProvider: self.metadataProvider,
                                                       path: options.path,
+                                                      processedPath: options.processedPath,
+                                                      livePhotoMoviePath: options.livePhotoMoviePath,
                                                       cameraSessionDelegate: self.delegate)
       photoOutput.capturePhoto(with: photoSettings, delegate: photoCaptureDelegate)

       // Assume that `takePhoto` is always called with the same parameters, so prepare the next call too.
-      photoOutput.setPreparedPhotoSettingsArray([photoSettings], completionHandler: nil)
+      // Live Photos need a fresh movie file URL for every capture, so don't reuse those settings.
+      if !options.livePhoto {
+        photoOutput.setPreparedPhotoSettingsArray([photoSettings], completionHandler: nil)
+      }
+    }
+  }
+
+  private func getRawPixelFormat(for rawMode: String, availableFormats: [OSType]) -> OSType? {
+    guard !availableFormats.isEmpty else {
+      return nil
+    }
+
+    if #available(iOS 14.3, *) {
+      if rawMode == "proRaw" {
+        return availableFormats.first(where: { AVCapturePhotoOutput.isAppleProRAWPixelFormat($0) })
+      }
+
+      return availableFormats.first(where: { AVCapturePhotoOutput.isBayerRAWPixelFormat($0) })
     }
+
+    return rawMode == "raw" ? availableFormats.first : nil
+  }
+
+  private func getRawSupportDescription(for rawMode: String, availableFormats: [OSType]) -> String {
+    if availableFormats.isEmpty {
+      return "\(rawMode) capture is not supported by this camera configuration."
+    }
+
+    if #available(iOS 14.3, *) {
+      let hasProRaw = availableFormats.contains(where: { AVCapturePhotoOutput.isAppleProRAWPixelFormat($0) })
+      let hasBayerRaw = availableFormats.contains(where: { AVCapturePhotoOutput.isBayerRAWPixelFormat($0) })
+
+      if rawMode == "raw" && hasProRaw && !hasBayerRaw {
+        return "Bayer RAW is not supported by this camera configuration, but Apple ProRAW is available."
+      }
+      if rawMode == "proRaw" && hasBayerRaw && !hasProRaw {
+        return "Apple ProRAW is not supported by this camera configuration, but Bayer RAW is available."
+      }
+    }
+
+    return "\(rawMode) capture is not supported by this camera configuration."
   }
 }
diff --git a/node_modules/react-native-vision-camera/ios/Core/Extensions/AVCaptureDevice+toDictionary.swift b/node_modules/react-native-vision-camera/ios/Core/Extensions/AVCaptureDevice+toDictionary.swift
index 4d8bc38..09b528b 100644
--- a/node_modules/react-native-vision-camera/ios/Core/Extensions/AVCaptureDevice+toDictionary.swift
+++ b/node_modules/react-native-vision-camera/ios/Core/Extensions/AVCaptureDevice+toDictionary.swift
@@ -26,7 +26,7 @@ extension AVCaptureDevice {
       "minExposure": minExposureTargetBias,
       "maxExposure": maxExposureTargetBias,
       "isMultiCam": isMultiCam,
-      "supportsRawCapture": false, // TODO: supportsRawCapture
+      "supportsRawCapture": position == .back,
       "supportsLowLightBoost": isLowLightBoostSupported,
       "supportsFocus": isFocusPointOfInterestSupported,
       "hardwareLevel": HardwareLevel.full.jsValue,
diff --git a/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift b/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift
index e845b38..ab8a387 100644
--- a/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift
+++ b/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift
@@ -16,16 +16,25 @@ class PhotoCaptureDelegate: GlobalReferenceHolder, AVCapturePhotoCaptureDelegate
   private let cameraSessionDelegate: CameraSessionDelegate?
   private let metadataProvider: MetadataProvider
   private let path: URL
+  private let processedPath: URL?
+  private let livePhotoMoviePath: URL?
+  private var livePhotoResult: [String: Any]?
+  private var rawPhotoResult: [String: Any]?
+  private var didFinish = false

   required init(promise: Promise,
                 enableShutterSound: Bool,
                 metadataProvider: MetadataProvider,
                 path: URL,
+                processedPath: URL? = nil,
+                livePhotoMoviePath: URL? = nil,
                 cameraSessionDelegate: CameraSessionDelegate?) {
     self.promise = promise
     self.enableShutterSound = enableShutterSound
     self.metadataProvider = metadataProvider
     self.path = path
+    self.processedPath = processedPath
+    self.livePhotoMoviePath = livePhotoMoviePath
     self.cameraSessionDelegate = cameraSessionDelegate
     super.init()
     makeGlobal()
@@ -42,15 +51,20 @@ class PhotoCaptureDelegate: GlobalReferenceHolder, AVCapturePhotoCaptureDelegate
   }

   func photoOutput(_: AVCapturePhotoOutput, didFinishProcessingPhoto photo: AVCapturePhoto, error: Error?) {
-    defer {
-      removeGlobal()
-    }
     if let error = error as NSError? {
-      promise.reject(error: .capture(.unknown(message: error.description)), cause: error)
+      finish(with: .failure(error))
       return
     }

     do {
+      if path.pathExtension.lowercased() == "dng" && !photo.isRawPhoto {
+        guard let processedPath else { return }
+        try FileUtils.writePhotoToFile(photo: photo,
+                                       metadataProvider: metadataProvider,
+                                       file: processedPath)
+        return
+      }
+
       try FileUtils.writePhotoToFile(photo: photo,
                                      metadataProvider: metadataProvider,
                                      file: path)
@@ -63,7 +77,7 @@ class PhotoCaptureDelegate: GlobalReferenceHolder, AVCapturePhotoCaptureDelegate
       let orientation = getOrientation(forExifOrientation: cgOrientation)
       let isMirrored = getIsMirrored(forExifOrientation: cgOrientation)

-      promise.resolve([
+      var result: [String: Any] = [
         "path": path.absoluteString,
         "width": width as Any,
         "height": height as Any,
@@ -72,26 +86,84 @@ class PhotoCaptureDelegate: GlobalReferenceHolder, AVCapturePhotoCaptureDelegate
         "isRawPhoto": photo.isRawPhoto,
         "metadata": photo.metadata,
         "thumbnail": photo.embeddedThumbnailPhotoFormat as Any,
-      ])
+      ]
+
+      if let processedPath {
+        result["processedPath"] = processedPath.absoluteString
+      }
+
+      if let livePhotoMoviePath {
+        result["livePhotoMoviePath"] = livePhotoMoviePath.absoluteString
+        livePhotoResult = result
+      } else if processedPath != nil {
+        rawPhotoResult = result
+      } else {
+        finish(with: .success(result))
+      }
     } catch let error as CameraError {
-      promise.reject(error: error)
+      finish(with: .failure(error))
     } catch {
-      promise.reject(error: .capture(.unknown(message: "An unknown error occured while capturing the photo!")), cause: error as NSError)
+      finish(with: .failure(error))
     }
   }

-  func photoOutput(_: AVCapturePhotoOutput, didFinishCaptureFor _: AVCaptureResolvedPhotoSettings, error: Error?) {
-    defer {
-      removeGlobal()
+  func photoOutput(_: AVCapturePhotoOutput,
+                   didFinishProcessingLivePhotoToMovieFileAt _: URL,
+                   duration _: CMTime,
+                   photoDisplayTime _: CMTime,
+                   resolvedSettings _: AVCaptureResolvedPhotoSettings,
+                   error: Error?) {
+    if let error = error as NSError? {
+      finish(with: .failure(error))
     }
+  }
+
+  func photoOutput(_: AVCapturePhotoOutput, didFinishCaptureFor _: AVCaptureResolvedPhotoSettings, error: Error?) {
     if let error = error as NSError? {
       if error.code == -11807 {
-        promise.reject(error: .capture(.insufficientStorage), cause: error)
+        finish(with: .failure(CameraError.capture(.insufficientStorage)))
       } else {
-        promise.reject(error: .capture(.unknown(message: error.description)), cause: error)
+        finish(with: .failure(error))
       }
       return
     }
+
+    if let rawPhotoResult {
+      finish(with: .success(rawPhotoResult))
+      return
+    }
+
+    guard let livePhotoMoviePath else {
+      return
+    }
+
+    guard FileManager.default.fileExists(atPath: livePhotoMoviePath.path),
+          let livePhotoResult else {
+      finish(with: .failure(CameraError.capture(.unknown(message: "Live Photo movie file was not created."))))
+      return
+    }
+
+    finish(with: .success(livePhotoResult))
+  }
+
+  private func finish(with result: Result<[String: Any], Error>) {
+    guard !didFinish else {
+      return
+    }
+
+    didFinish = true
+    removeGlobal()
+
+    switch result {
+    case .success(let value):
+      promise.resolve(value)
+    case .failure(let error as CameraError):
+      promise.reject(error: error)
+    case .failure(let error as NSError):
+      promise.reject(error: .capture(.unknown(message: error.description)), cause: error)
+    case .failure(let error):
+      promise.reject(error: .capture(.unknown(message: error.localizedDescription)), cause: error as NSError)
+    }
   }

   private func getOrientation(forExifOrientation exifOrientation: CGImagePropertyOrientation) -> String {
diff --git a/node_modules/react-native-vision-camera/ios/Core/Types/TakePhotoOptions.swift b/node_modules/react-native-vision-camera/ios/Core/Types/TakePhotoOptions.swift
index c569e88..6f3e211 100644
--- a/node_modules/react-native-vision-camera/ios/Core/Types/TakePhotoOptions.swift
+++ b/node_modules/react-native-vision-camera/ios/Core/Types/TakePhotoOptions.swift
@@ -11,9 +11,13 @@ import Foundation
 struct TakePhotoOptions {
   var flash: Flash = .off
   var path: URL
+  var processedPath: URL?
   var enableAutoRedEyeReduction = false
   var enableAutoDistortionCorrection = false
   var enableShutterSound = true
+  var rawMode = "off"
+  var livePhoto = false
+  var livePhotoMoviePath: URL?

   init(fromJSValue dictionary: NSDictionary) throws {
     // Flash
@@ -32,11 +36,26 @@ struct TakePhotoOptions {
     if let enable = dictionary["enableShutterSound"] as? Bool {
       enableShutterSound = enable
     }
+    // RAW capture mode: "off", "raw" (Bayer RAW) or "proRaw" (Apple ProRAW)
+    if let mode = dictionary["rawMode"] as? String {
+      rawMode = mode
+    }
+    // Live Photo capture
+    if let enable = dictionary["livePhoto"] as? Bool {
+      livePhoto = enable
+    }
     // Custom Path
+    let fileExtension = rawMode == "off" ? "jpg" : "dng"
     if let customPath = dictionary["path"] as? String {
-      path = try FileUtils.getFilePath(customDirectory: customPath, fileExtension: "jpg")
+      path = try FileUtils.getFilePath(customDirectory: customPath, fileExtension: fileExtension)
     } else {
-      path = try FileUtils.getFilePath(fileExtension: "jpg")
+      path = try FileUtils.getFilePath(fileExtension: fileExtension)
+    }
+    if rawMode != "off" {
+      processedPath = try FileUtils.getFilePath(fileExtension: "jpg")
+    }
+    if livePhoto {
+      livePhotoMoviePath = try FileUtils.getFilePath(fileExtension: "mov")
     }
   }
 }
diff --git a/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts b/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts
index 2e4c2f2..5f39c85 100644
--- a/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts
+++ b/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts
@@ -37,6 +37,13 @@ export interface TakePhotoOptions {
      * @default true
      */
     enableShutterSound?: boolean;
+    /**
+     * Whether to capture a Live Photo paired video alongside this photo.
+     *
+     * @platform iOS
+     * @default false
+     */
+    livePhoto?: boolean;
 }
 /**
  * Represents a Photo taken by the Camera written to the local filesystem.
@@ -56,6 +63,8 @@ export interface PhotoFile extends TemporaryFile {
      * Whether this photo is in RAW format or not.
      */
     isRawPhoto: boolean;
+    /** Processed JPEG captured alongside a RAW/DNG photo. (iOS only) */
+    processedPath?: string;
     /**
      * Display orientation of the photo, relative to the Camera's sensor orientation.
      *
@@ -67,6 +76,12 @@ export interface PhotoFile extends TemporaryFile {
      */
     isMirrored: boolean;
     thumbnail?: Record<string, unknown>;
+    /**
+     * The paired movie path when this photo was captured as a Live Photo.
+     *
+     * @platform iOS
+     */
+    livePhotoMoviePath?: string;
     /**
      * Metadata information describing the captured image. (iOS only)
      *
diff --git a/node_modules/react-native-vision-camera/src/types/PhotoFile.ts b/node_modules/react-native-vision-camera/src/types/PhotoFile.ts
index 85098d6..04872ba 100644
--- a/node_modules/react-native-vision-camera/src/types/PhotoFile.ts
+++ b/node_modules/react-native-vision-camera/src/types/PhotoFile.ts
@@ -38,6 +38,13 @@ export interface TakePhotoOptions {
    * @default true
    */
   enableShutterSound?: boolean
+  /**
+   * Whether to capture a Live Photo paired video alongside this photo.
+   *
+   * @platform iOS
+   * @default false
+   */
+  livePhoto?: boolean
 }

 /**
@@ -58,6 +65,12 @@ export interface PhotoFile extends TemporaryFile {
    * Whether this photo is in RAW format or not.
    */
   isRawPhoto: boolean
+  /**
+   * Processed JPEG captured alongside a RAW/DNG photo.
+   *
+   * @platform iOS
+   */
+  processedPath?: string
   /**
    * Display orientation of the photo, relative to the Camera's sensor orientation.
    *
@@ -69,6 +82,12 @@ export interface PhotoFile extends TemporaryFile {
    */
   isMirrored: boolean
   thumbnail?: Record<string, unknown>
+  /**
+   * The paired movie path when this photo was captured as a Live Photo.
+   *
+   * @platform iOS
+   */
+  livePhotoMoviePath?: string
   /**
    * Metadata information describing the captured image. (iOS only)
    *
